# Wails build output
/build/bin

# Frontend build output (keep the placeholder so the go:embed compiles)
/frontend/dist/*
!/frontend/dist/index.html
/frontend/node_modules

# Wails-generated bindings (regenerated by `wails dev` / `wails generate module`)
/frontend/wailsjs
